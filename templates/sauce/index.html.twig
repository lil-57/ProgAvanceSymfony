{% extends 'base.html.twig' %}

{% block title %}🌶️ Nos Sauces{% endblock %}

{% block content %}
<div class="text-center mb-4">
    <h1>🌶️ Nos Sauces</h1>
    <a href="{{ path('home') }}" class="btn btn-secondary">← Retour à l'accueil</a>
</div>

{% if sauces is empty %}
    <div class="text-center">
        <p>Aucune sauce disponible pour le moment.</p>
    </div>
{% else %}
    <div class="row">
        {% for sauce in sauces %}
            <div class="col-md-3 mb-4">
                <div class="card pollos-animation">
                    <div class="card-body text-center">
                        <h5 class="card-title">{{ sauce.name }}</h5>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endif %}
{% endblock %}